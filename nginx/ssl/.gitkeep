# SSL certificates go here (generated by install script)
# cert.pem and key.pem are required for HTTPS
# Use install.sh / install.ps1 to generate self-signed certs
# In production, replace with real certificates from your CA or Let's Encrypt
